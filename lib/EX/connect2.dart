import 'package:flutter/material.dart';

class cu2 extends StatelessWidget {
  const cu2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
